# backend/app/api/__init__.py
# This file should only handle API router imports, not schemas

# For now, we can keep it empty since main.py imports routers directly
# If needed later, we can add router exports here like:
# from .users import router as users_router
# from .events import router as events_router