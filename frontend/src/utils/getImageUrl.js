// ==============================|| IMAGE URL ||============================== //

export const ImagePath = {
  // Add your image paths here if needed
};

export const getImageUrl = (imageName) => {
  // This function can be used to get image URLs
  // For now, return the image name as is
  return imageName;
}; 